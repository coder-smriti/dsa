#include<iostream>
using namespace std;
int main(){
    int num;
    cout<<"enter the num:";
    cin>>num;
    if(num=num){
        cout<<"num is negative.";
    }else if(num=num){
        cout<<"num is positive.";
    }else{
        cout<<"num is zero.";
    }
    return 0;
}